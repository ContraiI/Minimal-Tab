# 更新日志

## 2026-07-05 (v0.9.1)

### 修复
- 壁纸来源选择器初始化时多处 const TDZ 错误，改用 `document.getElementById` 内联 + 字面量字符串
- 本地壁纸与 Bing 壁纸轮换频率相互独立存储（`wallpaperRotation` / `bingRotation`），切换来源时 UI 同步更新
- Bing 壁纸每日午夜强制刷新（独立定时器），不受轮换控件"不进行轮换"影响
- "恢复默认"后壁纸遮罩滑块轨道填充色同步更新
- Bing 缩略图列表高亮改用索引匹配
- 壁纸来源快速切换竞态条件修复（fetch 回调校验 source）

## 2026-07-05 (v0.9.0)

### 新增
- 壁纸来源选择器（无 / 本地壁纸 / Bing壁纸三段式），默认"无"（纯黑背景，遮罩/模糊/轮换隐藏）；Bing壁纸每日获取图片列表（横向滚动+滚轮），自动应用首张，支持壁纸轮换；切回本地壁纸时恢复上次壁纸（无则默认 bg.webp）；新增 localStorage 键 `wallpaperSource`、`bingWallpaperUrl`、`bingWallpaperDate`、`bingWallpaperList`

### 修改
- 侧边栏透明度滑块可调节范围从 20%-100% 扩展为 0%-100%
- 自定义搜索引擎编辑按钮移至右侧与开关同组（`.engine-toggle-right` 容器），避免 `justify-content: space-between` 下被挤到中间
- 侧边栏滚动条改为 5px 细条半透明样式，`scrollbar-gutter: stable` 预留空间防止内容跳动
- 预设主题色调整：删除藏青色 `#0f766e`，黄色改为 `#eab308`，保留深灰 `#374151`（9 预设 + 取色器）
- 搜索框 `border-color` 移除过渡动画，取色器拖拽时边框颜色即时响应

### 新增
- 搜索框下拉列表（历史记录/搜索建议）支持键盘 ↑↓ 键选择，Enter 确认选中项，Esc 关闭下拉；新增 `.history-item.active` 样式和 `dropdownSelectedIndex` 状态变量
- 壁纸管理页面工具栏右侧实时显示 localStorage 空间占用（KB/MB），随壁纸增删自动更新
- 侧边栏浅色模式滚动条颜色适配（半透明黑色替代白色）
- 时钟分区新增独立颜色控制（9 预设 + 取色器），时钟颜色通过 `--clock-color` CSS 变量与主题色解耦；新增 localStorage 键 `clockColor`
- 搜索框新增独立颜色控制（9 预设 + 取色器，位于启用搜索框开关下方随折叠），影响搜索框边框和 placeholder 文字；通过 `--search-color` / `--search-color-rgb` CSS 变量控制；新增 localStorage 键 `searchColor`
- 时钟与搜索框颜色联动按钮（链接图标，默认开启），拖拽取色器时双方实时同步预览；中间横杠显隐表示联动状态；浅色模式自动适配深色图标；新增 localStorage 键 `clockSearchLink`
- 搜索框聚焦时 placeholder 自动淡出隐藏（`opacity 0.2s` 过渡动画）
- 取色器面板未确认直接关闭设置或切换面板时，自动回退到打开前的颜色

### 修复
- 壁纸导入始终转为 JPEG 并限制体积 ≤400KB（质量从 0.85 自动递降至 0.3），避免原图已是高压缩 JPEG 时重新编码膨胀数倍或 PNG 等格式占用过大导致 localStorage 配额溢出
- `applyWallpaper` 保存 `customBg` 时增加 try-catch 保护，避免 setItem 异常导致未捕获错误
- 存储空间显示在历史清空为空时不再跳过更新，并增加 `localStorage.getItem` 空值保护
- 自定义搜索引擎删除/保存后侧边栏开关列表未刷新导致看似无法删除；`openCustomEngineForm` 编辑已删除引擎时重置为添加模式
- 默认引擎的自定义搜索引擎禁止删除（编辑表单隐藏删除按钮 + 删除处理器守卫提示）
- 重置设置后时钟位置异常（`applyClockCustomPos` 覆盖了 `applyClockFollow` 的定位样式，调换为先 custom pos 再 follow 的顺序）
- 链接按钮固定白色不受主题色影响，浅色模式自动切换深色 `#333`/`#111`

### 美化
- 壁纸管理弹窗全面重构美化：
  - 工具栏分组（导入 | 轮换/恢复/清空）+ SVG 图标 + 存储空间磁盘图标，新按钮类体系（primary/secondary/danger）
  - 标题加粗 + 主题色下划线和左竖条装饰
  - 缩略图卡片阴影 + 圆角 8px + hover 浮起效果（translateY(-3px)）+ 当前壁纸双圈光晕
  - 当前壁纸徽章（主题色标签 + 对号 + "当前"文字，右上角）
  - 删除按钮改为右上角圆形 X（毛玻璃效果 + hover 变红）+ 确认对话框
  - 编辑模式勾选框弹簧动画弹出 + 未勾选项缩略图变暗
  - 空状态大图标居中显示

## 2026-07-04 (v0.8.5)

### 新增
- **新标签页打开搜索结果开关**：搜索功能面板新增"在新标签页中打开搜索结果"开关，默认启用；关闭后搜索结果在当前标签页打开而非新标签页；新增 localStorage 键 `openInNewTab`
- **取色器**：主题色预设末尾新增彩虹色块，点击展开取色面板（动态尺寸方形色盘 + 竖条色相栏 + 十六进制输入框 + 确定按钮）；拖拽/输入时实时预览主题色，确定后保存并自动折叠，关闭面板自动还原；色盘圆环和色条横线位置指示器根据背景亮度自适应对比色；面板展开/折叠有过渡动画；确认文字颜色随主题色亮度切换黑白；提取公共 `hslToRgb` 函数消除重复代码；修复页面加载时取色器覆盖主题色为红色的 bug
- **搜索建议接口**：搜索功能面板新增"搜索建议接口"下拉选择器，支持不使用/百度/Google/必应四种选项，默认不使用；开启后输入时通过 `fetch()` + `host_permissions` 从对应 API 获取搜索补全词（100ms 防抖），在历史下拉框中显示；建议为空或 API 失败时静默降级显示搜索历史；百度接口返回 GBK 编码，通过 `TextDecoder('gbk')` 解码；输入框清空时自动显示历史记录（若历史开关开启）；新增 localStorage 键 `suggestionProvider`；manifest.json 新增 `host_permissions` 授权 API 域名
- **语言偏好**：个性化面板新增语言偏好分区，支持中文/English 切换；创建 `lang.js` 翻译模块（`I18N_DATA` 中英文映射表、`t()` 查找函数、`setLanguage()` 切换函数）；HTML 静态文本通过 `data-i18n` 属性标记；JS 动态文本通过 `t(key)` 调用；语言偏好持久化到 localStorage `language` 键
- **时钟位置下拉菜单**：五段固定定位分段控件改为下拉菜单（复用壁纸轮换 `.rotate-dropdown` 样式），解决英文文本溢出问题
- **时钟自定义位置**：下拉菜单新增"自定义"选项，选中后可鼠标拖拽/触屏拖动时钟到任意视口位置；下方展开坐标面板显示 X/Y 像素值（可手动输入）；锁按钮（双 SVG 图标切换）可切换锁定/解锁状态，锁定后禁止拖拽和输入
- **时钟右键快捷菜单**：自定义模式下右键时钟弹出菜单，含 X/Y 坐标输入框和确认按钮 + 锁定/解锁按钮；锁定状态下输入框禁用变灰且确认不生效
- 新增 localStorage 键：`clockCustomX`、`clockCustomY`、`clockCustomLocked`
- 自定义右键菜单（半透明毛玻璃）：壁纸区域右键弹出，包含"切换壁纸"（顺序切换至下一张，优先轮换池，无池则从历史切换）和"设置"（打开侧边栏），交互元素上保留浏览器默认菜单
- "重置搜索框"按钮移至重置设置面板，面板改为无分区布局，"初始化设置"置顶红色文本 + 备注说明
- 搜索框开关切换时渐入渐出动画

### 修改
- **壁纸轮换逻辑重构**：轮换只从轮换池中选取，移除全部历史 fallback；导入壁纸不再自动加入轮换池；轮换池不足 2 张时强制锁定为"不进行轮换"并提示"请先配置轮换池"；删除壁纸导致池不足 2 张时自动关闭轮换；编辑确认后池不足 2 张时强制关闭轮换；页面初始化时若池不足 2 张静默重置轮换设置；当前壁纸不在新池中时立即触发轮换
- 批量编辑模式下预选改为仅预选池内壁纸（空池不再预选全部历史）；"壁纸轮换"按钮固定宽度避免文本切换时跳动
- 恢复默认壁纸时同步停止轮换定时器
- 数字时钟默认 CSS 隐藏避免加载闪烁，JS 初始可见时渐入显示
- 重置设置面板重构：移除分区标题，按钮改为红色文本，各按钮下方备注说明
- 批量编辑模式下勾选圆圈从右上角移至左上角
- 未勾选时圆圈不显示对号，仅勾选后显示（`::after` 伪元素）

### 修复
- 语言切换时 `refreshI18n` 调用 `renderHistoryList` 导致搜索历史下拉自动弹出（改为仅在下拉已打开时才刷新）
- 自定义引擎表单在语言切换时被 `engineManager.innerHTML = ''` 销毁（`refreshI18n` 重建前先调用 `closeCustomEngineForm` 将表单移出）
- `closeCustomEngineForm` 中 `insertBefore` 因目标节点非直接子元素而静默失败（改为 `engineManager.parentNode.insertBefore`）
- 右键菜单锁按钮切换后未同步菜单内输入框的 `disabled` 状态
- 锁定状态下菜单确认按钮仍可修改坐标（添加 `isClockLocked()` 守卫）

### 删除
- `search-icon.svg`（代码中无引用的孤立文件）

## 2026-07-03 (v0.8.4)

### 新增
- 左侧导航选中项浮动高亮块动画（0.12s 平滑滑动），文本跟随主题色，左侧竖条指示器
- 导航项间距从 2px 增至 8px，圆角改为四面 4px
- 滑块轨道左侧填充主题色渐变，JS `updateSliderTrack()` 实时更新；轨道 `transition: 0.08s` 平滑跟随
- 滑块头空心样式：白色中心 + 主题色 2px 边框（webkikt + moz）
- 所有滑块 `step="any"` 连续滑动，`data-step` 保留节点间距，`snapToNode()` 松手/滚轮自动对齐
- 数值显示跟随最近节点值并带单位：遮罩/透明度→%，模糊/偏移/宽度/圆角→px，尾部零自动隐藏
- 壁纸轮换改为系统时钟对齐（以 0 时为基准）：1h 每整点、6h 于 0/6/12/18 时、12h 于 0/12 时、24h 于 0 时，setTimeout 动态调度
- 壁纸轮换池管理：壁纸管理弹窗新增"壁纸轮换"按钮 → 批量编辑模式 → 勾选壁纸 → 确认保存；无勾选默认全部参与；删除/导入壁纸自动同步轮换池
- 立即轮换按钮（壁纸轮换下拉左侧），点击立即随机切换
- 左侧导航新增"重置设置"项，右侧显示重置面板（说明文字 + 恢复默认设置按钮）

### 修改
- 自定义主题色功能移除（色块 + hex 输入 + 颜色预览全部删除），仅保留 10 个预设
- 壁纸缩略图去除底部"更改壁纸"文字遮罩，点击缩略图本身打开管理弹窗
- 文本重命名：外观模式→整体外观，遮罩→壁纸遮罩，模糊→壁纸模糊，上下移动→垂直偏移，左右移动→水平偏移，搜索框长度→搜索框宽度
- 浅色模式滑块轨道颜色 `#ccc` → `#777`
- 代码优化：提取 `roundToNode`/`getNodeStep`/`formatSliderVal`/`snapToNode`/`sliderWheelStep` 辅助函数；8 个滚轮处理器硬编码 min/max 改为读取元素属性；移除 8 处冗余 change 事件派发；4 个搜索滑块 IIFE 统一 wheel 逻辑

### 修复
- 浅色模式侧边栏选中项残留 `background` 样式冲突（改为仅 `.nav-highlight` 控制）
- 重置函数缺少滑块轨道填充更新和 display 同步

## 2026-07-01 (v0.8.3)

### 新增
- "搜索功能"面板新增 "搜索框" 分区（位于最上方），含四个滑块：上下移动（-300~300px）、左右移动（-300~300px）、搜索框长度（500~1200px）、搜索框圆角（0~25px），通过 CSS 变量控制
- 搜索框 CSS 变量默认值统一在 `:root` 中定义（`--search-offset-y/x`、`--search-width`、`--search-radius`）
- 搜索框分区新增"启用搜索框"开关，默认开启；关闭时标签页搜索框（visibility hidden 保留占位）和侧边栏滑块同步隐藏，滑块折叠/展开含 max-height + opacity 过渡动画
- 新增"重置搜索框"按钮，一键恢复搜索框样式默认值
- `.container` 的 `max-width` 改为 `max(800px, var(--search-width))`，搜索框超过 800px 时容器同步扩宽居中
- 个性化面板"数字时钟"开关下方新增"跟随搜索框"开关（默认开启），开启时展开"时钟位置"选择器（搜索框上方/下方），关闭时展开五段时钟位置选择器（左上/右上/居中/左下/右下，使用 fixed 定位到页面角落）
- 三个时钟控件级联显示：时钟 OFF → 全隐藏；时钟 ON + 跟随 OFF → 显示五段位置选择器；时钟 ON + 跟随 ON → 显示上方/下方选择器，均含折叠动画

### 修改
- 侧边栏导航 "搜索引擎" 改名为 "搜索功能"
- "搜索功能"面板中 "搜索引擎"和"默认引擎" 两个分区改为可折叠，默认折叠，状态持久化到 localStorage，折叠/展开含 max-height + opacity 过渡动画
- "搜索框"分区移至面板最上方
- "搜索历史"开关从"更多设置"移至"搜索功能"面板，"默认引擎"分区下方，加分隔线
- 个性化面板"数字时钟"开关上方加分隔线
- 搜索框分区与"搜索引擎"分区之间加分隔线
- 搜索框长度滑块最低值从 100 改为 500

### 删除
- 移除自定义取色器按钮（取色笔）及隐藏的 `<input type="color">`，hex 手动输入保留
- 移除日期显示功能（HTML 元素、CSS、JS 逻辑、开关、localStorage 键值全部清除）

## 2026-06-30

### 清理
- 移除缩略图重复 click 监听、无用 `importBtn` 变量、`try/catch`、空 CSS `.ce-btn-wrap`
- 重置处理用 localStorage 键名常量替代硬编码字符串

## 2026-06-30 (v0.8.2)

### 新增
- 侧边栏新增 "个性化" 面板，数字时钟开关从 "主题/壁纸" 面板移入
- 个性化面板新增 "侧边栏" 分区，含透明度滑块（0.20~1.00）+ 毛玻璃滑块（0~10px），侧边栏 `backdrop-filter: blur()` 由 `--sidebar-blur` 变量控制
- 侧边栏深浅模式背景改用 `--sidebar-opacity` CSS 变量控制；浅色模式文字颜色根据透明度动态加深（`--sidebar-main-rgb` / `--sidebar-nav-rgb` / `--sidebar-label-rgb`）
- 壁纸自动轮换：自定义下拉（带展开/收回动画），支持不轮换 / 每 1h / 6h / 12h / 24h，默认不轮换

### 修改
- 壁纸缩略图区域整体可点击打开壁纸管理（原仅底部按钮可点击）
- 侧边栏深色/浅色模式背景改为不透明（`rgba(20,20,20,0.95)` → `#141414`，`rgba(240,240,240,0.97)` → `#f0f0f0`）
- 导航项文字颜色改为实色（`rgba` → hex）
- "已启用" 分区标签改名为 "搜索引擎"
- 添加/编辑自定义搜索引擎从 modal 弹窗改为侧边栏内下拉展开表单，动画为 max-height 滑入
- 表单精准定位：添加模式在"手动添加"按钮下方展开，编辑模式在对应引擎项下方展开
- 关闭表单时自动归位到 engineManager 外部，防止 `innerHTML = ''` 重建时被销毁
- 清理弹窗迁移后的孤儿 CSS（`.modal-field`、`.file-input-wrap` 及其浅色变体）

## 2026-06-30 (v0.8.1)

### 新增
- 开关按钮弹性缓动动画 + 点击反馈（`:active` 缩放/拉伸）
- 深浅模式切换全局平滑过渡（20+ 选择器，`0.35s ease`）
- `syncEngineManager()` / `syncDefaultEngineManager()` 增量同步函数

### 修复
- 自定义引擎表单展开动画不生效 + 部分情况无法展开 + 点击外部不能关闭
- 搜索引擎侧边栏开关动画不生效：用增量 `checked` 同步代替全量 DOM 重建

### 修改
- 滑动开关过渡时长 0.2s → 0.25s；复选框行添加 `data-engine` 属性

## 2026-06-30 (v0.8.0)

### 新增
- 右侧边栏面板（双列 480px 布局），替代下拉菜单式设置界面
- 主题/壁纸面板：三段外观模式选择器、10 预设 + 自定义主题色取色器、壁纸缩略图 + 遮罩/模糊滑块
- 搜索引擎面板：引擎启用/禁用列表 + 默认引擎选择（合并原搜索引擎与默认引擎面板）
- 全局 `--accent` / `--accent-rgb` CSS 变量
- 面板切换淡入淡出、弹窗弹出、取色面板展开等动画
- 创建 `PROJECT_SUMMARY.md`

### 修改
- 设置按钮改为展开/收起右侧边栏，所有设置功能迁移至侧边栏
- "壁纸" 更名为 "主题/壁纸"；"外观模式"上移为面板首行，区名"整体外观"
- 遮罩滑块、数字时钟开关迁至侧边栏

### 修复
- 侧边栏开关 `:checked` 样式缺失
- 搜索引擎分区"手动添加"按钮重复

### 删除
- 旧下拉菜单 HTML/CSS/JS 全部移除

## 2026-05-31

### 新增
- 导入壁纸时校验文件类型和大小（最大 5MB），失败时弹出红色提示
- 自定义搜索引擎图标上传增加格式和大小校验（仅支持 SVG，最大 512KB），失败时弹出提示

### 修改
- 默认壁纸从 JPEG 转为 WebP 格式，体积从 151KB 降至 36KB
- 浅色模式下遮罩透明度滑块的轨道颜色变浅，不再与深色背景混淆

### 优化
- 统一设置菜单中所有按钮的样式（圆角、悬停变色效果），一级和二级菜单保持一致
- 二级菜单中开关按钮移至最右端，与文字标签左右分开
- 去除二级菜单按钮之间的分割线

### 删除
- 移除夜间模式初始化时重复添加样式类的冗余代码
- 移除已被 CSS 覆盖的 JavaScript 内联样式
- 移除一处永远不生效的函数调用
- 移除未使用的常量
- 移除两条无作用的 CSS 规则
