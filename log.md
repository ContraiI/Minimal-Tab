# 更新日志

## 2026-09-16 (v1.4.5)

### 优化
- **翻译过的内容不再重复翻译**：网页翻译此前逐段请求翻译接口，且没有任何结果留存——改了语言、页面重新渲染，或是在单页应用里点了链接又点回来，同一段文字都得重新翻一遍，等一遍。现在同一段文字只要在本浏览器本次会话里翻过，再遇到就直接用已翻好的结果（不区分页面，别的网页上出现同一句话也直接命中）；同一页里多处相同的文字（导航、按钮、菜单）也合并成一次请求，不再各请求一次。翻译接口有并发上限，省下来的名额会让真正的新内容翻得更快。翻译引擎配置一变，旧结果立即失效，不会拿旧配置的译文顶替。命中已有结果时，原文末尾那个转圈的加载图标也不会再闪一下。

### 新增
- **右键悬浮球展开「清除缓存」按钮**：清除译文缓存并重译当前页——在悬浮球上点右键，球的下方会展开一个**刷子按钮**，点它就是清除缓存并重译；再右键一次收回，按住拖球也会自动收回。按钮与悬浮球同规格：一样大的 32px 圆形、同样的圆角与阴影，只是图标换成刷子；底色是中性灰、**比球更透明一些**，**不跟随翻译开关变色**（球是蓝是灰都一样），鼠标指向它时**变浅**而不是变暗。悬停变浅的幅度是单独调过的：两个按钮透明度不同（球 92%、刷子 70%），照抄球的悬停色会让刷子看着"几乎没变"，但按透明度折算满又会让本来就更透明的刷子**发飘**（像要淡出），所以最终取在中间——比悬浮球略轻一档，但一眼能看出变浅了。两个按钮**彼此独立**：鼠标碰到刷子不会让翻译球跟着变色（反过来也一样），刷子的透明度也不受球影响。球贴着屏幕底部时按钮会翻到球的正上方，不会跑到屏幕外。悬浮球本身的原生悬停提示也一并去掉了——它弹出来的位置正好落在按钮上。
- **清缓存会报出清掉了多少条**：点刷子按钮后，页面顶部会提示「已清除 12 条译文缓存，正在重新翻译」（翻译没开时是「已清除 12 条译文缓存」），条数是这次真正清掉的译文条数，为 0 时说明本来就没有缓存。
- **翻译边栏的设置里也能清缓存**：「网页翻译」分区底部多了一行纯文字的**「清除缓存」**（只有文字，没有按钮框）。点一下即清空译文缓存并让当前标签页重译一遍，**提示与悬浮球那颗刷子完全一样**——页面顶部弹出同一条「已清除 N 条译文缓存（，正在重新翻译）」黑底提示，2 秒后淡出；清的是同一份全局缓存，只是入口换到了设置里。（目标页面本身跑不了扩展脚本时，如浏览器内置页面，这条提示无处可弹，属于该入口的已知取舍。）

### 修复
- **清缓存后看不到结果提示、翻译开着时也不会重译**：清缓存之后本该在页面顶部弹一句结果提示，但负责弹提示的那个函数在 v1.4.5 开发中只写了调用、漏了实现，一调用就报错。受影响的不只是那句提示：翻译开着时，紧跟在提示后面的「还原原文 + 重新翻译」也被这次报错跳过了，于是清缓存看起来什么都没发生（翻译关着时本来就只清缓存，所以只表现为没有提示）。现已补齐实现——顶部居中的黑底提示，2 秒后淡出移除，并把这次清掉的条数写在提示里。

### 调整
- **悬浮球的悬停反馈由变深改为变浅**：以前鼠标指向悬浮球，是把它从 92% 不透明变成完全不透明，蓝色因此显得更实、更深；现在改成直接换一档更浅的底色——蓝球 `#2563eb → #4b8bf5`，翻译停用时的灰球 `#5a5f6b → #8a909b`。悬停色的深浅按各自的不透明度单独取值（球 0.92、刷子 0.7），不照抄同一个色值。
- **悬浮球默认位置再下移**：默认位置由右上角 `top:16px` 一路下移到 `top:96px`，避开多数网页顶部那条固定导航栏，也给展开的按钮留出落点。只影响从未拖动过悬浮球的用户，已经摆好位置的保持原样。
- **隐私政策补充了译文缓存的说明**：上一条的译文缓存会在浏览器内存里暂存最近翻过的「原文 → 译文」配对（不写磁盘、不上传、关浏览器即消失），因此隐私政策新增了「网页翻译的临时缓存」一节，逐条说明它存什么、存在哪、容量上限、何时失效、如何随时清除，避免与原文「这些内容不会被存储」的表述冲突。

## 2026-09-15 (v1.4.4)

### 修复
- **清除已移除翻译引擎遗留的本地凭证**：腾讯云翻译引擎在 v1.4.2 移除后，此前配置过它的老用户设备上仍留着一份历史密钥（SecretId / SecretKey，连同账号地域与配置锁定状态）。这些键不再被任何功能读取，界面上也没有删除入口，只会在你设备上一直躺着。现在每次打开新标签页时会自动清除（扩展页面存储与后台存储里的副本一并清掉），无需手动操作，也不影响任何其他设置。另需留意：如果你导出过较早版本的配置备份，那份文件里可能仍有明文密钥，建议自行删除。
- **译文预览不再对冷门语言显示英文**：翻译设置里「线条样式」各项第二行的预览文本会跟随目标语言，但此前只给 23 种常用语言配了样本，其余 18 种（乌克兰语、印尼语、希伯来语、孟加拉语、斯瓦希里语等）一律显示英文 “Translation preview”，看起来像是目标语言没生效。现在没有配样本的语言改为显示该语言的母语名（如 Українська、Kiswahili、עברית），预览文字与字形始终是目标语言本身。

### 调整
- **悬浮球默认位置移到右上角**：网页翻译悬浮球的默认位置由右下角改为右上角。已经拖动过悬浮球的用户不受影响，仍保留你摆放的位置；想放到别处直接拖动即可。
- **关掉悬浮球开关会一并清除自定义位置**：关闭「悬浮球」开关即视为放弃此前拖动的摆放位置，下次重新开启时回到默认的右上角，而不是跳回原来的旧位置。此前遗留的「已关闭但仍保存着坐标」状态也会在打开设置时自动清掉。

### 优化
- **拼音搜索不再拖慢新标签页**：用于「全拼」匹配的那张索引表（约 54KB）此前在每次打开新标签页时都会解析一遍，哪怕你根本没搜索——多数会话确实用不到它。现在改成第一次用到拼音搜索时才解析，解析写法本身也换成了更快的实现（实测快约 18 倍），解析完还会把那份数据释放掉。搜索行为与之前完全一致。

### 重构
- **颜色逻辑合并为一份共享实现**：新标签页、翻译边栏、扩展弹窗原本各自写了一份颜色换算，以及一份「主题色 → 界面配色」的换算；同名逻辑散在三处，改一处容易漏掉另两处。现在统一抽成共享模块（三个纯函数 + 一处配色换算），三个界面由同一份实现决定配色。外观与操作方式完全不变，只是主题色取值异常时不再把无效值写进配色（改为回落到默认蓝色）。
- **取色器合并为一份共享实现**：新标签页里的三个取色器（主题色、时钟色、搜索框颜色）与翻译边栏里的两个（译文文字色、边框颜色）此前是两份各自演化的实现，同一个交互改进要改两遍、还容易只改一边。现在合并成一份共享实现，两边的差异（面板怎么展开、颜色存到哪里）改为配置项。拖动时实时预览、松手或点确定才真正保存、取消或关闭回到原来的颜色、以及主题色与时钟/搜索框颜色的联动，行为都与之前完全一致。
- **取色面板的结构与样式也合并了**：取色器展开后的那块面板（画板 + 色相条 + 输入框 + 确定按钮）以及旁边那个彩虹色块，此前在两个页面里各写了一份结构相同的标记和一套样式，调一次样式要改两处。现在面板与色块都由共享实现自动生成，色块本身（以及选中时的白色描边）的基础样式也只留一份，两边的展开方式也统一了；顺带删掉一条从来没有生效过的样式规则。位置、观感与操作都不变，唯一可见的细微差别是新标签页取色器的十六进制输入框在聚焦时边框会有一个 0.2 秒的渐变（翻译边栏本来就是这个表现）。
- **折叠分区的标题与箭头样式也合并了**：设置面板里可折叠分区的标题栏、以及标题右侧那个会随展开/收起旋转的小箭头，此前在新标签页与翻译边栏各写了一份（箭头那份超长的图标数据还被抄了三份）。现在合到一份里，两处标题的排版与箭头动画由同一处决定，不会再出现一边改了另一边没跟上的情况。观感不变，只顺带让新标签页的箭头补上了"宽度不参与压缩"这一定义（翻译边栏本来就有，避免标题较长时箭头被挤小）。
- **最后两处小重复也清掉了**：一是「当前系统是深色还是浅色」的判断——此前三个地方各写一份，其中一处每次判断都重新查询一次系统设置，现在合成一份、每个页面只查一次，跟随系统切换主题的监听也走同一处；二是给元素设置 CSS 变量的那个 4 行小函数，此前在翻译边栏与网页翻译脚本里各有一份逐字相同的实现。行为与之前完全一致。

## 2026-09-14 (v1.4.3)

### 新增
- **自定义引擎图标不再只支持 SVG**：手动添加搜索引擎时，PNG / JPG / WebP / GIF / ICO 等常见图片都能直接当图标用；位图会自动缩放到够用的尺寸再保存，图大也不会拖慢新标签页或撑爆本地存储。
- **不透明图片自动提取剪影**：白底 JPG 这类没有透明通道的图标，会自动抠出图形轮廓，用于搜索框未聚焦时的白色剪影和下拉列表里的图标，不会再糊成一个方块。
- **导出可自选是否包含密钥**：「备份与恢复」新增「导出时包含密钥」勾选框，默认关闭——导出配置默认不含 Microsoft / 自定义引擎的 API Key（已停用引擎的历史残留密钥同样不会写入），勾选后才会写入备份；勾选状态会被记住，不必每次重设。备份文件一旦包含密钥即为明文，请妥善保管、不要上传或分享。

### 修复
- **修复壁纸模糊拉高后窗口四周的白边**：模糊会让壁纸图层边缘渐变成透明，从而在四周露出一圈白边；现在开启模糊时壁纸会随模糊值向外扩一点，把这段渐变推到屏幕之外，并把页面底色改成深色兜底。外扩量随模糊值连续变化，从 0 调到 0.5 不会出现壁纸跳变，模糊拉满时的整体缩放也控制在 4% 以内。
- **修复编辑当前引擎图标不即时生效**：改完正在使用的自定义引擎图标后，搜索框左侧的图标会立即刷新，不必再刷新页面。
- **修复「备份与恢复」漏掉网页翻译设置**：网页翻译的目标语言、呈现方式（仅译文/双语对照）、悬浮球开关与位置、译文样式（字体色/线条色/斜体/加粗/线条样式）此前不随备份走——这些设置只存在扩展的后台存储里，导出时没收进备份，导入后仍然是旧值；现在导出会一并收进备份、导入时一起还原（旧版本导出的备份仍能正常导入）。
- **修复「恢复默认设置」可能复位不彻底**：此前重置只清存储、再逐项手工复位界面，漏掉了「导出时包含密钥」勾选框、时钟「自定义位置」的锁定按钮与坐标输入框（重置后仍显示锁定，再点一下还会反向锁回去）、「搜索引擎」与「默认引擎」两个分区的收起状态等，表现为「存储已清空、界面还是旧样子」。现在重置改为清空全部设置（保留语言）后整页刷新重建，界面必然与存储一致。注意：重置后页面会刷新一次，侧边栏会收起。
- **修复浅色模式下「删除」按钮悬停时文字消失**：自定义搜索引擎表单里的「删除」按钮，浅色模式悬停时背景变红、文字却仍是红色，两个字糊在红底上看不出来（浅色模式的文字色规则权重高于悬停规则，把悬停要变的白字压住了）。现在悬停统一为红底白字，与深色模式一致。
- **修复搜索建议下拉可能自己弹回来**：在搜索框输入后若马上点到别处，稍后才返回的建议会把下拉重新弹出来，打断你正在做的事；快速输入时先后到达的响应也可能用旧关键词的建议盖住新的。现在过期响应会被直接丢弃，只有与你当前输入匹配的结果才会显示。
- **修复浅色主题色下弹窗按钮看不清**：把主题色设成黄色、浅绿这类浅色时，扩展弹窗里的按钮是白字压浅底、不好认；现在弹窗也会按亮度自动换成深色文字，与新标签页和翻译边栏的表现一致。

### 调整
- **「添加 / 更新」按钮悬停只把背景压暗**：自定义搜索引擎表单里的保存按钮，悬停不再换成固定的深蓝色，改为只把按钮背景压暗一档、"添加 / 更新"文字保持纯白。这样把主题色设成红、绿等非蓝色时，悬停不会再突然串成蓝色。
- **搜索按钮悬停时放大镜不再变暗**：鼠标移到搜索框右侧的圆形按钮上时，放大镜图标保持纯白，不再变成浅灰；搜索框聚焦时圆圈仍为你的主题色。

### 优化
- **拖动取色器更跟手**：在翻译设置里拖动色盘 / 色相条时，不再每次鼠标移动都写一遍配置并触发所有已打开网页重涂，改为节流写入、松手（或取消 / 确定）时补写最终颜色。拖动过程的实时预览不变，但侧边栏与被翻译页面明显更少卡顿。
- **网页翻译更省资源**：扫描待翻译内容时改为先做最便宜的判断再逐级深入（是否有待译属性 → 是否在可视区 → 是否可见 → 最后才查祖先链），滚动、翻页、信息流不断刷新时不再为每个屏外/无关节点做昂贵检查；拖动页面上的翻译悬浮球时也不再因反复读取布局而掉帧。取色器拖动结束后，鼠标即使在窗口外松开也会正确收尾，不会再出现「不按键移动鼠标颜色自己变」。
- **时钟不再空转**：数字时钟只在真正显示时才每秒走表。搜索框聚焦（时钟自动隐藏）或关掉时钟开关时，不再每秒改写一次看不见的时钟内容，也不再重复查找同一个元素。
- **长时间翻译不再让页面越用越占内存**：开着「翻译此页」连刷信息流、或在不刷新页面的站点里反复切换页面时，页面丢弃掉的旧内容所留下的翻译记录（以及被它们连带钉住、无法回收的旧页面结构）现在会被及时清理。实测修复前连续滚动 7 分钟会累积 600 多条文本记录、1100 多条属性记录，其中约 85%~89% 指向早已被页面丢弃的内容——这些都是无用负担。静态文章页不受影响（本来就没有可回收的内容）。
- **清理永不生效的代码与样式**：删除两个从未被使用的文案键、一处已无消费者的全局导出，以及 5 处永不生效的 CSS 规则（例如没有任何使用点的 `.sidebar-divider`、恒被内联样式压住的时钟跟随定位规则、与基础规则取值完全相同的空操作块、同块内重复的声明），消除后续误改的隐患。

## 2026-09-09 (v1.4.2)

### 新增
- **取色实时预览**：字体颜色、边框颜色取色面板里拖动色盘/色相条或输入 hex 时，设置内的预览译文与网页上的双语译文都会即时变色；点“确定”落定，直接收起则回退原色。
- **“恢复默认设置”覆盖翻译**：在新标签页重置设置时，翻译引擎/语言与网页翻译（目标语言、呈现方式、译文样式、悬浮球）也一并清回默认，已打开的翻译边栏会立即按默认重建。

### 修复
- **修复新标签页可能白屏/脚本中断**：壁纸轮换相关常量声明顺序导致的报错，重启新标签页不再抛错。
- **修复导出配置可能泄漏已停用引擎凭证**：旧版腾讯云密钥继续排除在配置备份之外。

### 调整
- **翻译设置交互对齐新标签页**：下拉列表展开时按钮不再变色填充；开关行按下时整行闪现主题色。
- **译文样式展开更顺滑**：展开有过渡、收回即收；标题随色块一起出现、不再错位或向左右扩张；线条样式选中项左侧圆点中心改为白色。

### 移除
- **移除腾讯云翻译引擎**：删除腾讯云 TMT 引擎及设置项（含账号地域/密钥配置），并清理其文案、引用与旧并发特判。

## 2026-09-08 (v1.4.1)

### 修复
- **翻译配置切换更可靠**：修改翻译引擎或凭证时，会先还原当前页面译文再重新翻译，避免译文被重复翻译、关闭翻译后无法恢复原文，或旧请求影响新配置。
- **翻译请求不再无限等待**：翻译请求现在最多等待 20 秒，即使服务返回响应头后响应内容卡住，也会结束请求并释放页面翻译资源。
- **翻译失败提示更具体**：侧栏会区分请求超时、网络错误、接口返回异常、请求过于频繁、服务端故障、认证失败和其它请求拒绝，排查问题更直观。
- **后台翻译配置更新更准确**：快速连续修改翻译设置时，不会再让较早的设置读取结果覆盖最新配置。

### 优化
- **Bing 翻译认证请求去重**：多个翻译请求同时需要刷新 Bing 认证信息时，只会合并为一次认证请求，减少重复网络请求。
- **空译文处理更稳妥**：接口成功但返回空译文时，不再把该内容标记为失败；同时避免对同一内容反复提交请求。
- **翻译任务状态隔离**：旧翻译请求完成时不会误删新请求的加载图标或等待状态。

### 调整
- **腾讯云限流问题暂不处理**：本次更新未加入腾讯云动态并发、限流识别或自动重试；腾讯云接口的限流与失败问题仍需单独定位和处理。

## 2026-08-31 (v1.4.0)

### 新增
- **腾讯云翻译引擎**：新增腾讯云机器翻译（TMT），支持 SecretId、SecretKey 与地域配置；凭证输入默认隐藏，避免旁观泄露
- **引擎连接测试**：Microsoft Translator、腾讯云和自定义 OpenAI 兼容引擎可在保存前测试连接，并显示本次请求耗时
- **引擎配置确认与锁定**：编辑内容先保留为草稿，点击“确定”后才保存并锁定；点击“修改”可重新编辑，避免未完成配置被误用

### 优化
- **腾讯云批量翻译更稳定**：腾讯云请求改为平滑排队发送，遇到限频或临时服务错误会自动退避重试，长页面不再只翻译开头几段
- **译文样式层级更清晰**：六种线条选项收纳进默认折叠的“线条样式”子菜单，并补充缩进、引导线和键盘焦点保护
- **设置界面层次与布局**：设置页面滑入滑出时增加左侧阴影；连接状态与紧凑按钮改为同行右对齐
- **语言切换实时同步**：翻译边栏动态字段、区域下拉、测试结果及工具栏弹窗会随中英文切换立即更新
- **备份保护敏感凭证**：导出配置时自动排除 Microsoft API Key、腾讯云 SecretId/SecretKey 和自定义接口 API Key

### 修复
- **翻译引擎冷启动准确**：后台首次处理网页翻译前会等待已保存引擎配置加载完成，不再偶发使用默认引擎
- **连接测试结果可靠**：测试期间阻止重复操作，避免旧请求覆盖新结果；腾讯云 API 错误会保留真实错误码
- **网页翻译配置恢复**：补全凭证后，先前失败的网页文本会自动重新尝试翻译
- **全局翻译队列准确结算**：修复多条翻译请求同时启动时可能发生的任务结果错配或请求悬挂

### 修改
- **隐私政策更新**：补充腾讯云翻译、凭证本地保存、连接测试与备份排除敏感凭证的说明

## 2026-08-29 (v1.3.0)

### 新增
- **网页翻译按标签页独立开关**：翻译开关只作用于当前标签页，其他标签页互不影响；刷新或跳转页面后自动恢复该页翻译状态
- **切换呈现方式时关闭全部翻译**：切换「仅译文 / 双语对照」时所有标签页停止翻译，避免不同呈现方式混杂；切换目标语言或引擎则保持翻译并自动重译
- **翻译请求全局限流**：多标签页同时翻译时请求统一排队（最多 10 个同时），不再打爆翻译接口

### 重构
- 翻译开关与限流调度更稳健：单个请求出错不再阻塞后续；修复对已关闭标签页操作可能产生的异常

### 修改
- **更新隐私政策**：补充说明网页翻译开关按标签页独立、只在浏览器会话内保存（关闭浏览器即清除），启用翻译仅影响当前标签页

## 2026-08-28 (v1.2.3)

### 修复
- **字体颜色不再影响线条颜色**：设置字体颜色时，译文的下划线 / 边框不会跟着变；未自定义线条颜色时统一为灰色
- **线条颜色全局统一**：调整线条颜色会同步应用到所有带线条的译文样式，设置预览与网页翻译保持一致

### 修改
- 更新线条颜色设置说明，明确它对所有带线条的样式生效

### 重构
- 精简译文样式的存储与渲染逻辑，统一线条颜色的保存与加载

## 2026-08-25 (v1.2.2)

### 新增
- **译文样式设置**：双语对照模式下新增「译文样式」区块，可分别设置字体颜色、线条颜色（下划线 / 边框）、斜体与粗体
- **译文呈现样式可选**：无、三种下划线、两种边框共 6 种，每项带实时预览
- **交互与滚动优化**：设置面板、下拉列表带淡入动画，开关与选项行悬停高亮；设置区与结果区改用更细的滚动条，内容滚动不跳动

### 重构
- 精简译文样式相关代码，移除未使用的函数与死样式

## 2026-08-23 (v1.2.1)

### 新增
- **网页翻译「双语对照」模式**：新增「呈现方式」设置，可选「仅译文」或「双语对照」（原文下方保留译文，便于对照阅读）；切换模式会自动重新翻译；链接提示、图片说明等属性文本两种模式下均为替换
- **搜索历史匹配优化**：历史记录按匹配质量智能排序，命中的文字高亮显示；无匹配时显示全部历史
- **翻译加载动画**：正在翻译的文字末尾显示旋转加载图标，翻译完成自动消失

### 修复
- **取色器颜色准确**：修复色盘中「所见与所取颜色不一致」的问题，点击哪里就取到哪里
- **悬浮球拖动不再误触发开关**：拖动悬浮球结束不会误切换翻译开关

### 修改
- **悬浮球尺寸缩小**：悬浮球 44px → 32px，图标 22px → 16px

### 重构
- 搜索历史匹配重写为按质量打分排序；精简并统一多处重复代码，全面补充中文注释

### 已知局限
- 内联元素（如「Hello **world**」）中的译文分行可能错位；flex / grid 布局下的裸文本换行可能异常（边缘情况）

## 2026-08-21 (v1.2.0)

### 新增
- **网页翻译**：在任意网页将原文翻译为目标语言（源语言自动检测），完整保留加粗、链接等行内格式；滚动、缩放或网页动态加载内容时自动增量翻译
- **覆盖特殊结构**：支持弹出层内部文本、SVG 文字、图片与链接的提示文本
- **智能跳过**：自动跳过代码块、输入框、可编辑区域与页头元数据；页面语言与目标语言一致时不发翻译请求
- **翻译设置整合**：边栏翻译与网页翻译共用一套引擎设置，网页翻译分区可单独设置目标语言与悬浮球开关
- **「翻译此页」按钮**：popup 工具栏一键开启 / 关闭网页翻译
- **「翻译此页」悬浮球**：网页内可拖拽，点击切换翻译开关，位置自动记忆

### 修复
- **扩展更新后网页翻译不再报错**：旧翻译脚本失效时静默停止，不再产生未捕获异常

### 重构
- 边栏与网页翻译统一为单一引擎配置，精简重复代码；翻译设置界面控件与标签页风格统一

## 2026-08-19 (v1.1.1)

### 修复
- **「切换壁纸」按来源切换**：右键菜单「切换壁纸」按当前壁纸来源工作——来源为「无」时提示未启用，「本地壁纸」与「Bing壁纸」分别在各自列表中切换
- **修复翻译输入框自动全选失效**：点击输入框自动全选文本时，不再被鼠标点击干扰

### 修改
- 翻译输入框聚焦时自动全选已有文本，方便整体替换
- 移除 popup 启动器提示文案

## 2026-08-16 (v1.1.0)

### 新增
- **翻译模块**：浏览器右侧翻译边栏，通过工具栏 popup 打开，跨标签页保持；输入停顿 600ms 自动翻译，源 / 目标语言可选 42 种并支持一键调换
- **4 种翻译引擎**：Google（免费）、Microsoft 官方接口（需 API Key）、微软免费接口（自动获取访问令牌）、自定义 OpenAI 兼容引擎（任意 API 地址与模型）
- **视觉与设置跟随**：界面风格跟随新标签页设置（主题色、深浅模式、中英文），翻译设置独立存储

### 修复
- 切换语言后下拉按钮文字不更新的问题
- 自定义引擎拉取模型列表失败时给出友好提示，不再报裸错误
- 残留的无效引擎配置导致扩展崩溃的问题
- 语言名称在部分情况下不显示的问题
- 微软引擎「区域」改为下拉选择，避免手动输入出错

### 修改
- 语言列表显示为「本地名(母语名)」格式（如「英语(English)」）

## 2026-07-12 (v1.0.0)

### 修复
- Bing壁纸列表按地址去重，同一壁纸不重复出现
- 删除自定义引擎后搜索不再报错、侧边栏不再残留无效条目

### 修改
- 删除当前正在使用的自定义引擎时，自动切换回默认引擎
- 搜索框图标与下拉菜单边框跟随搜索框颜色设置
- 自定义引擎只需上传一个图标，配置更简单

## 2026-07-05 (v0.13.1)

### 修复
- 本地壁纸与 Bing 壁纸的轮换频率可独立设置
- Bing壁纸每日午夜自动刷新一次
- 恢复默认设置后壁纸遮罩滑块显示同步更新
- Bing壁纸列表中当前壁纸高亮准确
- 快速切换壁纸来源时不再显示错乱

## 2026-07-05 (v0.13.0)

### 新增
- **壁纸来源选择**：「无 / 本地壁纸 / Bing壁纸」三种来源可选；Bing壁纸每日自动获取列表并应用，切回本地壁纸自动恢复上次使用的壁纸，支持定时轮换
- **时钟与搜索框独立配色**：时钟颜色、搜索框颜色均可单独调整，不再跟随主题色；两者可开启联动，调整一个时另一个同步变化
- **搜索建议下拉支持键盘操作**：↑↓ 选择、Enter 确认、Esc 关闭
- **壁纸管理显示存储占用**：工具栏实时显示已用空间

### 修复
- 导入壁纸自动转为 JPEG 并压缩，不再撑爆存储空间
- 修复删除自定义引擎后开关列表不刷新
- 默认引擎的自定义搜索引擎不可删除
- 修复重置设置后时钟位置错乱

### 修改
- **壁纸管理弹窗全面改版**：工具栏分组与图标、缩略图卡片悬浮效果、当前壁纸徽章、删除确认、批量编辑动画、空状态提示
- **侧边栏透明度范围扩大**：0%–100%
- 预设主题色调整
- 侧边栏滚动条改为细条样式

## 2026-07-04 (v0.12.0)

### 新增
- **取色器**：预设颜色末尾新增彩虹取色，支持色盘 / 色相条 / 十六进制输入，实时预览
- **搜索建议接口**：可选百度 / Google / 必应联想词，接口失败自动回退搜索历史
- **语言偏好**：界面支持中文 / English 切换
- **时钟增强**：位置改为下拉菜单；新增「自定义」模式可拖拽时钟到任意位置，支持坐标输入与锁定；自定义模式下右键时钟可精确调整
- **壁纸右键菜单**：右键壁纸区域弹出「切换壁纸」与「设置」
- **「在新标签页打开搜索结果」开关**：关闭后搜索结果在当前标签页打开
- **重置入口整理**：「重置搜索框」并入重置设置面板

### 修复
- 切换语言时搜索历史下拉不再自动弹出、自定义引擎表单不再丢失
- 锁定状态下无法再通过右键菜单修改时钟坐标，锁定按钮状态同步
- 恢复默认壁纸后轮换定时器同步停止

### 修改
- **壁纸轮换只从轮换池选取**：池不足 2 张时自动关闭轮换并提示
- **重置设置面板重构**：无分区布局、红色文本按钮、按钮下方备注说明
- 批量编辑勾选框移至左上角，未勾选不显示对号

### 移除
- 移除无引用的孤立图标文件

## 2026-07-03 (v0.11.0)

### 新增
- **左侧导航高亮动画**：选中项带悬浮高亮块与左侧竖条，跟随主题色
- **滑块体验升级**：拖动连续平滑、松手自动对齐预设档位、数值实时显示并带单位；轨道填充主题色
- **壁纸轮换池**：可勾选参与轮换的壁纸，删除 / 导入自动同步；新增「立即轮换」一键随机切换；轮换对齐整点调度
- **「重置设置」面板**：左侧导航新增入口，一键恢复默认

### 修复
- 修复浅色模式下侧边栏选中项颜色残留

### 修改
- 移除自定义主题色，仅保留 10 个预设
- 壁纸缩略图整体可点击，直接打开管理弹窗
- 设置项名称调整（外观模式→整体外观、遮罩→壁纸遮罩、模糊→壁纸模糊等）

### 重构
- 滑块相关代码精简为通用辅助函数

## 2026-07-01 (v0.10.0)

### 新增
- **搜索框样式调节**：新增「搜索框」分区，可调整位置（上下 / 左右）、宽度、圆角，并支持一键重置
- **「启用搜索框」开关**：关闭时隐藏搜索框及相关设置
- **时钟位置增强**：新增「跟随搜索框」开关，开启时可选择在搜索框上方或下方；关闭时可放到屏幕四角或居中

### 修改
- 「搜索引擎」面板更名「搜索功能」，分区可折叠（状态记忆）；「搜索历史」开关移入其中
- 搜索框长度范围调整为 500–1200px

### 移除
- 移除自定义取色器，保留十六进制输入
- 移除日期显示功能，时钟只保留时间

## 2026-06-30 (v0.9.1)

### 重构
- 清理搜索历史与重置相关冗余代码

## 2026-06-30 (v0.9.0)

### 新增
- **「个性化」面板**：数字时钟开关移入；新增侧边栏透明度与毛玻璃模糊滑块
- **壁纸自动轮换**：可选不轮换 / 每 1h / 6h / 12h / 24h，默认不轮换

### 修改
- 壁纸缩略图整体可点击打开管理
- 自定义引擎表单由弹窗改为面板内展开，添加 / 编辑定位更精准
- 分区名称调整；侧边栏背景改为不透明

## 2026-06-30 (v0.8.0)

### 新增
- 开关按钮弹性动画与点击反馈
- 深浅模式切换平滑过渡动画

### 修复
- 修复自定义引擎表单展开异常（动画不生效、部分情况无法展开、点击外部不能关闭）
- 修复搜索引擎开关动画不生效

## 2026-06-30 (v0.7.0)

### 新增
- **右侧边栏设置面板**：双列布局替代原下拉菜单式设置，含「主题 / 壁纸」「搜索引擎」面板，切换带过渡动画

### 修改
- 设置按钮改为展开 / 收起右侧边栏

### 修复
- 修复侧边栏开关样式缺失、「手动添加」按钮重复

### 移除
- 移除旧的下拉菜单式设置界面

## 2026-05-31 (v0.6.0)

### 新增
- **文件上传校验**：壁纸导入仅支持图片；自定义引擎图标仅支持 SVG（≤512KB），违规会提示

### 修改
- 默认壁纸改用 WebP 格式，体积从 151KB 降至 36KB
- 设置按钮样式统一（圆角、悬停变色），二级菜单开关移到最右端

### 重构
- 清理冗余代码、内联样式与无效 CSS
